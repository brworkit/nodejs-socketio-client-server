# socket-node-example
A socket client-server example.

A socket.io client and server example

Modules required:

socket.io + Express


To test it:

Start server:

node socket-server

Start client:

node socket-client